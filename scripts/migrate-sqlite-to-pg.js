/* Do not run against production without an approved maintenance window. */
const fs = require('fs');
const path = require('path');
const Database = require('better-sqlite3');
const { Client } = require('pg');

const sqlitePath = process.env.SQLITE_PATH || path.join(__dirname, '..', 'data', 'shop.db');
const databaseUrl = process.env.DATABASE_URL;
if (!databaseUrl) throw new Error('DATABASE_URL is required.');
if (!fs.existsSync(sqlitePath)) throw new Error(`SQLite source database was not found: ${sqlitePath}`);

const tables = {
  categories: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL UNIQUE, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  brands: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL UNIQUE, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  pack_sizes: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, size TEXT NOT NULL UNIQUE, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  roles: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL UNIQUE, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  customers: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL, email TEXT, phone TEXT, city TEXT, address TEXT, customer_code TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  suppliers: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, supplier_id TEXT NOT NULL UNIQUE, name TEXT NOT NULL, company_name TEXT, phone TEXT, email TEXT, address TEXT, opening_balance NUMERIC(12,2) NOT NULL DEFAULT 0, credit_limit NUMERIC(12,2) NOT NULL DEFAULT 0, active INTEGER NOT NULL DEFAULT 1, notes TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, deleted_at TEXT`,
  company_settings: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, company_name TEXT NOT NULL DEFAULT 'Business Name', legal_business_name TEXT, business_type TEXT, company_registration_number TEXT, tax_number TEXT, industry_category TEXT, primary_phone TEXT, secondary_phone TEXT, email TEXT, website TEXT, whatsapp_number TEXT, address_line_1 TEXT, address_line_2 TEXT, area_locality TEXT, city TEXT, state_province TEXT, postal_code TEXT, country TEXT, logo_data_url TEXT, alternate_logo_data_url TEXT, document_header TEXT, document_footer TEXT, footer_tagline TEXT, terms_and_conditions TEXT, return_policy TEXT, payment_terms TEXT, general_notes TEXT, authorized_signature_name TEXT, authorized_signature_designation TEXT, signature_image_data_url TEXT, customer_support_contact TEXT, show_logo INTEGER NOT NULL DEFAULT 1, show_address INTEGER NOT NULL DEFAULT 1, show_phone INTEGER NOT NULL DEFAULT 1, show_email INTEGER NOT NULL DEFAULT 1, show_website INTEGER NOT NULL DEFAULT 1, show_tax_number INTEGER NOT NULL DEFAULT 1, show_authorized_signature INTEGER NOT NULL DEFAULT 1, currency TEXT NOT NULL DEFAULT 'GBP', currency_symbol TEXT NOT NULL DEFAULT '£', date_format TEXT NOT NULL DEFAULT 'DD/MM/YYYY', time_format TEXT NOT NULL DEFAULT '24H', time_zone TEXT NOT NULL DEFAULT 'UTC', decimal_places INTEGER NOT NULL DEFAULT 2, number_formatting TEXT NOT NULL DEFAULT '1,234.56', tax_registration_number TEXT, default_tax_configuration TEXT, tax_display_preference TEXT NOT NULL DEFAULT 'Exclusive', tax_inclusive_preference INTEGER NOT NULL DEFAULT 0, invoice_prefix TEXT NOT NULL DEFAULT 'INV', invoice_number_format TEXT NOT NULL DEFAULT 'INV-YYYYMM-######', starting_number INTEGER NOT NULL DEFAULT 1, number_of_digits INTEGER NOT NULL DEFAULT 6, invoice_terms TEXT, default_notes TEXT, landing_hero_text TEXT NOT NULL DEFAULT 'Premium essentials for a refined everyday routine\nPremium wellness essentials for modern life.', landing_marquee_text TEXT NOT NULL DEFAULT 'Fast Delivery / Trusted Care / Secure Checkout / Premium Products', landing_hero_title TEXT NOT NULL DEFAULT 'Premium essentials for a refined everyday routine', landing_hero_subtitle TEXT NOT NULL DEFAULT 'Premium wellness essentials for modern life.', landing_service_fast_delivery TEXT NOT NULL DEFAULT 'Fast Delivery', landing_service_trusted_care TEXT NOT NULL DEFAULT 'Trusted Care', landing_service_secure_checkout TEXT NOT NULL DEFAULT 'Secure Checkout', landing_service_premium_products TEXT NOT NULL DEFAULT 'Premium Products', social_facebook TEXT, social_youtube TEXT, social_snapchat TEXT, social_tiktok TEXT, social_pinterest TEXT, social_canva TEXT, social_whatsapp TEXT, social_instagram TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  products: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, sku TEXT NOT NULL UNIQUE, name TEXT NOT NULL, category_id BIGINT REFERENCES categories(id), brand TEXT, description TEXT, pack_size TEXT, unit TEXT, purchase_rate NUMERIC(12,2) NOT NULL DEFAULT 0, sale_rate NUMERIC(12,2) NOT NULL DEFAULT 0, tax_rate NUMERIC(12,2) NOT NULL DEFAULT 0, image_url TEXT, active INTEGER NOT NULL DEFAULT 1, opening_stock INTEGER NOT NULL DEFAULT 0, opening_stock_finalized INTEGER NOT NULL DEFAULT 0, opening_stock_locked_at TEXT, reorder_level INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, deleted_at TEXT`,
  product_images: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE, image_url TEXT NOT NULL, is_main INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  product_batches: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT REFERENCES products(id) ON DELETE CASCADE, batch_number TEXT NOT NULL, expiry_date TEXT, initial_quantity INTEGER NOT NULL DEFAULT 0, available_quantity INTEGER NOT NULL DEFAULT 0, purchase_cost NUMERIC(12,2) NOT NULL DEFAULT 0, supplier_name TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  stock_movement_ledger: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT NOT NULL REFERENCES products(id), batch_id BIGINT REFERENCES product_batches(id), transaction_type TEXT NOT NULL DEFAULT 'OPENING_STOCK', reference_type TEXT, reference_id BIGINT, quantity INTEGER NOT NULL DEFAULT 0, unit_cost NUMERIC(12,2) NOT NULL DEFAULT 0, amount NUMERIC(12,2) NOT NULL DEFAULT 0, notes TEXT, created_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  item_batch_ledger: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT NOT NULL REFERENCES products(id), batch_id BIGINT REFERENCES product_batches(id), transaction_type TEXT NOT NULL DEFAULT 'OPENING_STOCK', quantity INTEGER NOT NULL DEFAULT 0, unit_cost NUMERIC(12,2) NOT NULL DEFAULT 0, notes TEXT, created_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  opening_stock_entries: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT NOT NULL REFERENCES products(id), batch_id BIGINT REFERENCES product_batches(id), quantity INTEGER NOT NULL DEFAULT 0, unit_cost NUMERIC(12,2) NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'Finalized', finalized_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, locked_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, created_by TEXT, notes TEXT`,
  orders: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, order_number TEXT NOT NULL UNIQUE, customer_id BIGINT REFERENCES customers(id), customer_name TEXT, email TEXT, phone TEXT, city TEXT, delivery_address TEXT, order_total NUMERIC(12,2) NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'Pending', payment_status TEXT NOT NULL DEFAULT 'Unpaid', delivery_status TEXT NOT NULL DEFAULT 'Pending', confirmed_at TEXT, confirmed_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  order_items: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, order_id BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE, product_id BIGINT NOT NULL REFERENCES products(id), quantity INTEGER NOT NULL, unit_price NUMERIC(12,2) NOT NULL, line_total NUMERIC(12,2) NOT NULL`,
  sale_invoices: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, invoice_number TEXT NOT NULL UNIQUE, order_id BIGINT NOT NULL REFERENCES orders(id), customer_id BIGINT REFERENCES customers(id), invoice_date TEXT NOT NULL, subtotal NUMERIC(12,2) NOT NULL DEFAULT 0, discount NUMERIC(12,2) NOT NULL DEFAULT 0, tax NUMERIC(12,2) NOT NULL DEFAULT 0, total NUMERIC(12,2) NOT NULL DEFAULT 0, payment_status TEXT NOT NULL DEFAULT 'Unpaid', payment_method TEXT, paid_amount NUMERIC(12,2) NOT NULL DEFAULT 0, payment_reference TEXT, captured_at TEXT, delivery_status TEXT NOT NULL DEFAULT 'Pending', dispatch_number TEXT, dispatch_date TEXT, expected_arrival_date TEXT, dispatch_note TEXT, dispatched_by TEXT, dispatched_at TEXT, created_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  sale_invoice_items: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, invoice_id BIGINT NOT NULL REFERENCES sale_invoices(id) ON DELETE CASCADE, product_id BIGINT NOT NULL REFERENCES products(id), quantity INTEGER NOT NULL, unit_price NUMERIC(12,2) NOT NULL, line_total NUMERIC(12,2) NOT NULL`,
  order_item_batches: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, order_id BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE, invoice_id BIGINT REFERENCES sale_invoices(id), order_item_id BIGINT REFERENCES order_items(id), product_id BIGINT NOT NULL REFERENCES products(id), batch_id BIGINT REFERENCES product_batches(id), quantity INTEGER NOT NULL DEFAULT 0, allocated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  inventory_transactions: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, product_id BIGINT NOT NULL REFERENCES products(id), batch_id BIGINT REFERENCES product_batches(id), transaction_type TEXT NOT NULL, reference_type TEXT, reference_id BIGINT, quantity_in INTEGER NOT NULL DEFAULT 0, quantity_out INTEGER NOT NULL DEFAULT 0, unit_cost NUMERIC(12,2) DEFAULT 0, previous_quantity INTEGER DEFAULT 0, new_quantity INTEGER DEFAULT 0, created_by TEXT, updated_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT, notes TEXT`,
  purchases: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, purchase_number TEXT NOT NULL UNIQUE, supplier_id BIGINT NOT NULL REFERENCES suppliers(id), purchase_date TEXT NOT NULL, subtotal NUMERIC(12,2) NOT NULL DEFAULT 0, discount NUMERIC(12,2) NOT NULL DEFAULT 0, tax NUMERIC(12,2) NOT NULL DEFAULT 0, total NUMERIC(12,2) NOT NULL DEFAULT 0, paid_amount NUMERIC(12,2) NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'Draft', notes TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  purchase_items: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, purchase_id BIGINT NOT NULL REFERENCES purchases(id) ON DELETE CASCADE, product_id BIGINT NOT NULL REFERENCES products(id), quantity INTEGER NOT NULL, unit_cost NUMERIC(12,2) NOT NULL, discount NUMERIC(12,2) NOT NULL DEFAULT 0, tax NUMERIC(12,2) NOT NULL DEFAULT 0, line_total NUMERIC(12,2) NOT NULL`,
  purchase_returns: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, return_number TEXT NOT NULL UNIQUE, purchase_id BIGINT NOT NULL REFERENCES purchases(id), supplier_id BIGINT NOT NULL REFERENCES suppliers(id), return_date TEXT NOT NULL, reason TEXT, total NUMERIC(12,2) NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'Draft', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  purchase_return_items: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, purchase_return_id BIGINT NOT NULL REFERENCES purchase_returns(id) ON DELETE CASCADE, product_id BIGINT NOT NULL REFERENCES products(id), quantity INTEGER NOT NULL, unit_cost NUMERIC(12,2) NOT NULL, total NUMERIC(12,2) NOT NULL`,
  sale_returns: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, return_number TEXT NOT NULL UNIQUE, invoice_id BIGINT NOT NULL REFERENCES sale_invoices(id), order_id BIGINT REFERENCES orders(id), customer_id BIGINT REFERENCES customers(id), product_id BIGINT NOT NULL REFERENCES products(id), quantity INTEGER NOT NULL, unit_price NUMERIC(12,2) NOT NULL DEFAULT 0, return_reason TEXT, return_date TEXT NOT NULL, return_amount NUMERIC(12,2) NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'Pending', created_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  accounts: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL UNIQUE, type TEXT NOT NULL CHECK (type IN ('cash', 'bank', 'gateway_clearing', 'other')), opening_balance NUMERIC(12,2) NOT NULL DEFAULT 0, current_balance NUMERIC(12,2) NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  fund_transfers: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, from_account_id BIGINT NOT NULL REFERENCES accounts(id), to_account_id BIGINT NOT NULL REFERENCES accounts(id), amount NUMERIC(12,2) NOT NULL CHECK (amount > 0), transfer_date TEXT NOT NULL, reference_note TEXT, created_by TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, CHECK (from_account_id <> to_account_id)`,
  payment_ledger: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, order_id BIGINT REFERENCES orders(id), invoice_id BIGINT REFERENCES sale_invoices(id), customer_id BIGINT REFERENCES customers(id), supplier_id BIGINT REFERENCES suppliers(id), account_id BIGINT REFERENCES accounts(id), payment_reference TEXT, payment_method TEXT, entry_type TEXT NOT NULL DEFAULT 'Payment', amount NUMERIC(12,2) NOT NULL DEFAULT 0, balance NUMERIC(12,2) NOT NULL DEFAULT 0, entry_date TEXT NOT NULL, description TEXT, status TEXT NOT NULL DEFAULT 'Captured', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  supplier_ledger: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, supplier_id BIGINT NOT NULL REFERENCES suppliers(id), reference_type TEXT NOT NULL, reference_id BIGINT, entry_date TEXT NOT NULL, description TEXT, debit NUMERIC(12,2) NOT NULL DEFAULT 0, credit NUMERIC(12,2) NOT NULL DEFAULT 0, balance NUMERIC(12,2) NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  expenses: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, expense_date TEXT NOT NULL, category TEXT NOT NULL DEFAULT 'Miscellaneous', description TEXT NOT NULL, amount NUMERIC(12,2) NOT NULL DEFAULT 0, payment_method TEXT NOT NULL DEFAULT 'Cash', reference_no TEXT, created_by TEXT NOT NULL DEFAULT 'admin', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  users: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, email TEXT NOT NULL, password_hash TEXT NOT NULL, display_name TEXT NOT NULL, role_id BIGINT NOT NULL REFERENCES roles(id), active INTEGER NOT NULL DEFAULT 1, totp_secret TEXT, totp_enabled INTEGER NOT NULL DEFAULT 0, last_login_at TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  payment_events: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, provider TEXT NOT NULL, event_id TEXT NOT NULL, event_type TEXT NOT NULL, order_id BIGINT REFERENCES orders(id), payload TEXT NOT NULL, processed_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(provider, event_id)`,
  audit_logs: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, actor_user_id BIGINT REFERENCES users(id), action TEXT NOT NULL, entity_type TEXT NOT NULL, entity_id TEXT NOT NULL, old_value TEXT, new_value TEXT, ip_address TEXT, timestamp TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  ai_knowledge_base: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, title TEXT NOT NULL, content TEXT NOT NULL, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_by TEXT`,
  ai_assistant_settings: `id BIGINT PRIMARY KEY CHECK (id = 1), enabled INTEGER NOT NULL DEFAULT 1, marketing_tones TEXT NOT NULL DEFAULT '[]', updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_by TEXT`,
  ai_provider_settings: `id BIGINT PRIMARY KEY CHECK (id = 1), provider TEXT NOT NULL DEFAULT 'anthropic' CHECK (provider IN ('anthropic', 'openai')), api_key_encrypted TEXT, api_key_last4 TEXT, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_by TEXT`,
  agent_contact_requests: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT, contact_method TEXT NOT NULL, contact_value TEXT NOT NULL, message TEXT, status TEXT NOT NULL DEFAULT 'open', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`,
  customer_complaints: `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, complaint_number TEXT NOT NULL UNIQUE, customer_name TEXT, contact_method TEXT, contact_value TEXT, order_id BIGINT REFERENCES orders(id), description TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'open', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, resolved_at TEXT`,
  notification_settings: `id BIGINT PRIMARY KEY CHECK (id = 1), low_stock_enabled INTEGER NOT NULL DEFAULT 0, low_stock_emails TEXT NOT NULL DEFAULT '', low_stock_sms_phone TEXT NOT NULL DEFAULT '', complaint_enabled INTEGER NOT NULL DEFAULT 0, complaint_emails TEXT NOT NULL DEFAULT '', complaint_sms_phone TEXT NOT NULL DEFAULT '', contact_request_enabled INTEGER NOT NULL DEFAULT 0, contact_request_emails TEXT NOT NULL DEFAULT '', contact_request_sms_phone TEXT NOT NULL DEFAULT '', dispatch_internal_enabled INTEGER NOT NULL DEFAULT 0, dispatch_internal_emails TEXT NOT NULL DEFAULT '', dispatch_internal_sms_phone TEXT NOT NULL DEFAULT '', updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_by TEXT`
};
const indexes = [
  'CREATE INDEX IF NOT EXISTS idx_products_active ON products(active)',
  'CREATE INDEX IF NOT EXISTS idx_products_category ON products(category_id)',
  'CREATE INDEX IF NOT EXISTS idx_orders_status ON orders(status)',
  'CREATE INDEX IF NOT EXISTS idx_purchases_supplier ON purchases(supplier_id)',
  'CREATE INDEX IF NOT EXISTS idx_inventory_product ON inventory_transactions(product_id)',
  'CREATE INDEX IF NOT EXISTS idx_product_batches_product ON product_batches(product_id, expiry_date)',
  'CREATE INDEX IF NOT EXISTS idx_order_item_batches_order ON order_item_batches(order_id, product_id)',
  'CREATE UNIQUE INDEX IF NOT EXISTS uq_users_email_lower ON users(LOWER(email))'
];
const tableNames = Object.keys(tables);
const quote = (identifier) => `"${identifier.replace(/"/g, '""')}"`;

function preflight(source) {
  const violations = source.prepare('PRAGMA foreign_key_check').all();
  if (violations.length) throw new Error(`SQLite foreign-key preflight failed: ${violations.map((item) => `${item.table} rowid=${item.rowid} parent=${item.parent}`).join('; ')}`);
}
async function copyTable(source, target, tableName) {
  const columns = source.prepare(`PRAGMA table_info(${quote(tableName)})`).all().map((column) => column.name);
  if (!columns.length) throw new Error(`SQLite source is missing expected table: ${tableName}`);
  const rows = source.prepare(`SELECT * FROM ${quote(tableName)}`).all();
  const sql = `INSERT INTO ${quote(tableName)} (${columns.map(quote).join(', ')}) VALUES (${columns.map((_, index) => `$${index + 1}`).join(', ')}) ON CONFLICT DO NOTHING`;
  for (const row of rows) await target.query(sql, columns.map((column) => row[column]));
}
async function verifyCounts(source, target) {
  const mismatches = [];
  for (const tableName of tableNames) {
    const sqliteCount = Number(source.prepare(`SELECT COUNT(*) AS count FROM ${quote(tableName)}`).get().count);
    const pgCount = Number((await target.query(`SELECT COUNT(*)::bigint AS count FROM ${quote(tableName)}`)).rows[0].count);
    const matches = sqliteCount === pgCount;
    console.log(`${tableName}: SQLite=${sqliteCount}, PostgreSQL=${pgCount}, ${matches ? 'OK' : 'MISMATCH'}`);
    if (!matches) mismatches.push(`${tableName}: SQLite=${sqliteCount}, PostgreSQL=${pgCount}`);
  }
  if (mismatches.length) throw new Error(`Row-count verification failed: ${mismatches.join('; ')}`);
  console.log(`Verified row counts for ${tableNames.length} tables.`);
}
async function realignSequences(target) {
  for (const tableName of tableNames) await target.query(`SELECT setval(pg_get_serial_sequence($1, 'id'), COALESCE((SELECT MAX(id) FROM ${quote(tableName)}), 1), true)`, [tableName]);
}
async function main() {
  const source = new Database(sqlitePath, { readonly: true });
  const target = new Client({ connectionString: databaseUrl, ssl: process.env.PGSSL === 'true' ? { rejectUnauthorized: process.env.PGSSL_REJECT_UNAUTHORIZED !== 'false' } : undefined });
  await target.connect();
  try {
    preflight(source);
    await target.query('BEGIN');
    for (const [tableName, definition] of Object.entries(tables)) await target.query(`CREATE TABLE IF NOT EXISTS ${quote(tableName)} (${definition})`);
    for (const tableName of tableNames) await copyTable(source, target, tableName);
    for (const statement of indexes) await target.query(statement);
    await realignSequences(target);
    await verifyCounts(source, target);
    await target.query('COMMIT');
    console.log(`Migrated ${tableNames.length} tables from ${sqlitePath}.`);
  } catch (error) {
    await target.query('ROLLBACK');
    throw error;
  } finally {
    source.close();
    await target.end();
  }
}
main().catch((error) => { console.error(error.message); process.exitCode = 1; });